# 🎉 GPT-5 (Mindflow-Y) 最终使用指南

## ✅ 完整实现总结

经过多轮修复，GPT-5 Responses API 的原生 Agentic 能力现已完全可用。

---

## 📊 系统架构

```
用户 → Next.js (3000) → GPT-5 Service (8002) → OpenAI Responses API
                      ↓
                 语音服务 (8001)
```

---

## 🚀 启动服务

### 1. 启动 GPT-5 服务（端口 8002）

```bash
cd /Users/juntinghua/Desktop/agent
./start_gpt5_service.sh
```

或手动启动：
```bash
source venv_gpt5/bin/activate
python gpt5_service.py &
```

### 2. 启动 Next.js（端口 3000）

```bash
pnpm run dev
```

### 3. 验证

```bash
# GPT-5 服务
curl http://localhost:8002/health

# Next.js
curl http://localhost:3000
```

---

## 🎯 使用方式

### 在 Mindflow 中

1. **选择模型**：
   - **Mindflow-Y** - 快速响应（reasoning: low）
   - **Mindflow-Y-Pro** - 深度推理（reasoning: medium/high）

2. **启用工具调用**：打开工具调用开关

3. **发送消息**：任何需要搜索、计算或文档生成的任务

---

## 🛠️ 支持的工具

### 内置工具（GPT-5 原生）

- **web_search** - 网络搜索（自动执行，无需手动实现）

### 自定义工具（26个）

- search_web - 自定义搜索
- execute_code - 代码执行  
- generate_image - 图片生成
- create_document - 文档创建
- analyze_image - 图片分析
- ... 等 22 个工具

---

## 📝 测试示例

### 示例 1: 基本对话

```
你好，请介绍一下你自己
```

**预期**: 结构化回复（【快速结论】...）

### 示例 2: 搜索任务

```
请搜索 2024 年最新的 AI 技术发展并生成报告
```

**预期**:
- 🔧 多次 web_search 调用
- ✅ 搜索完成通知
- 📄 详细的分析报告（2000+ 字）

### 示例 3: 代码执行

```
用 Python 计算斐波那契数列前 10 项
```

**预期**:
- 🔧 调用 execute_code
- ✅ 返回计算结果

---

## 🔍 调试与监控

### 查看 GPT-5 服务日志

```bash
tail -f /Users/juntinghua/Desktop/agent/gpt5_service.log
```

### 成功的日志示例

```
📋 首条消息 content.type: input_text
✅ 确认：正确使用 input_text/output_text
📤 发送到 OpenAI Responses API
📥 收到响应: output 数组长度: 24
  🌐 内置 web_search: ... (11次)
✅ 解析结果: 文本长度=4550, web_search=11
✅ GPT-5 Responses API 调用成功
```

---

## ⚙️ 配置说明

### 环境变量

**.env** (GPT-5 服务):
```bash
OPENAI_API_KEY=your_key_here
GPT5_SERVICE_URL=http://localhost:8002
```

**.env.local** (Next.js):
```bash
OPENAI_API_KEY=your_key_here
GPT5_SERVICE_URL=http://localhost:8002
```

### 模型配置

| 模型 | Provider | Reasoning | Verbosity | 用途 |
|------|----------|-----------|-----------|------|
| Mindflow-Y | gpt5-thinking | low | medium | 快速响应 |
| Mindflow-Y-Pro | gpt5-pro | medium/high | high | 深度分析 |

---

## 🔧 核心技术实现

### 1. 消息格式规范

```json
{
  "input": [
    {"role": "user", "content": [{"type": "input_text", "text": "..."}]},
    {"role": "assistant", "content": [{"type": "output_text", "text": "..."}]}
  ]
}
```

### 2. 工具格式

```json
{
  "tools": [
    {"type": "web_search"},  // 内置
    {"type": "function", "name": "...", "description": "...", "parameters": {...}}  // 自定义
  ]
}
```

### 3. 响应解析

提取：
- `type: "message"` → 文本内容
- `type: "web_search_call"` → 搜索调用
- `type: "function_call"` → 自定义工具调用
- `type: "reasoning"` → 推理过程

### 4. 工具回路

**内置工具**: 自动执行，只需提取信息显示

**自定义工具**: 二段式回路
```
请求1 → function_call
  ↓
执行工具
  ↓  
请求2 (previous_response_id + tool_outputs)
  ↓
最终结果
```

---

## 📋 涉及的文件

### 核心文件
1. `/gpt5_service.py` - GPT-5 服务（809 行）
2. `/app/api/chat/route.ts` - Next.js 集成
3. `/app/chat/page.tsx` - 前端UI
4. `/start_gpt5_service.sh` - 启动脚本

### 配置文件
- `/.env` - GPT-5 服务配置
- `/.env.local` - Next.js 配置

---

## ✅ 已解决的问题

1. ✅ 工具格式错误（Chat Completions → Responses API）
2. ✅ role:tool 不被接受（转换为 assistant）
3. ✅ tool_calls 字段残留（彻底移除）
4. ✅ content type 错误（text → input_text/output_text）
5. ✅ 缺少 tool_outputs（二段式回路）
6. ✅ web_search_call 未显示（提取并转发）
7. ✅ item_type 未定义（变量作用域）
8. ✅ 请求超时（120s → 300s）
9. ✅ 错误提示包含服务商信息（简化为通用错误）
10. ✅ 端口冲突（迁移到 8002）

---

## 🎯 性能指标

### 响应时间

- **基本对话**: 2-5 秒
- **单次搜索**: 10-20 秒
- **复杂任务**（多次搜索 + 长报告）: 30-120 秒

### 超时配置

- **第一次请求**: 300 秒
- **续写请求**: 300 秒
- **流式请求**: 600 秒

---

## 🎊 功能特性

1. ✅ **原生 Agentic** - GPT-5 自主规划和执行
2. ✅ **内置 web_search** - 自动网络搜索
3. ✅ **26+ 自定义工具** - 完整工具生态
4. ✅ **推理可见** - reasoning 内容显示
5. ✅ **上下文管理** - previous_response_id
6. ✅ **工具调用显示** - 前端UI展示
7. ✅ **流式支持** - 实时响应
8. ✅ **错误处理** - 友好的错误提示

---

**GPT-5 原生 Agentic 能力完全可用！** 🚀

