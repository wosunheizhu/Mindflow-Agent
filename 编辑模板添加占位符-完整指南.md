# 编辑 Science project.odp 模板 - 添加 Carbone 占位符

## 🎯 问题

当前 Google Slides 模板：
- ❌ 首页标题没有显示（没有占位符）
- ✅ 内容页正常（已有占位符）

---

## 🔧 解决方案：编辑模板添加占位符

### 步骤1：安装 LibreOffice（如果还没有）

```bash
brew install --cask libreoffice
```

### 步骤2：打开模板文件

```bash
cd /Users/juntinghua/Desktop/agent
open -a "LibreOffice Impress" "Science project.odp"
```

### 步骤3：编辑首页（标题页）

1. **点击首页的标题文本框**
2. **全选文本**（Cmd+A）
3. **替换为**：`{d.title}`
4. **点击副标题文本框**
5. **替换为**：`{d.subtitle}`

**如果有其他文本框**：
- 作者：`{d.author}`
- 日期：`{d.date}`

### 步骤4：检查内容页（确认已有占位符）

1. **切换到第二页**（内容页）
2. **检查是否有占位符**，例如：
   - `{d.slides[i].title}`
   - `{d.slides[i].bullets[j]}`

如果没有，需要添加。

**典型的内容页占位符**：
```
标题框：{d.slides[i].title}

内容列表：
• {d.slides[i].bullets[j]}
```

### 步骤5：保存模板

**File** → **Save**（Cmd+S）

### 步骤6：重新上传到 Carbone

```bash
cd /Users/juntinghua/Desktop/agent

curl -X POST 'https://api.carbone.io/template' \
  -H "Authorization: Bearer eyJhbGciOiJFUzUxMiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiIxMjgwNTA3MDU0OTEyNzYxNzQ5IiwiYXVkIjoiY2FyYm9uZSIsImV4cCI6MjQyNDM2MzcyNiwiZGF0YSI6eyJ0eXBlIjoicHJvZCJ9fQ.AXMe7WXAYhGjU_7e4WkzUt0kZh6JMkm1LCisatVC8JUYsuYXG9rnf25JQ0VPSdxhPlcL13incPWhwmwD8Lukq5erAVT82zfx3B7IlsZWPlYFck70gnomK14NDHfUjzThydanBP5AhQ6mTLA7XiFmPwndJMoOhedIQmkf3IHLUHoO_gLg" \
  -H 'carbone-version: 4' \
  -F 'template=@Science project.odp' | jq

# 复制新的 templateId
```

### 步骤7：更新 Template ID

1. **更新代码**：
   - 编辑 `lib/carbone-ppt.ts`
   - 或更新 Vercel 环境变量 `CARBONE_PPT_TEMPLATE_ID`

2. **推送代码** 或 **Redeploy Vercel**

---

## 🎨 Carbone 占位符完整参考

### 基础占位符
```
{d.fieldName}           - 简单字段
{d.nested.field}        - 嵌套对象
{d.array[i]}            - 数组循环
{d.array[i].field}      - 数组对象字段
```

### 当前数据结构

我们传递给 Carbone 的数据：

```json
{
  "title": "演示文稿标题",
  "subtitle": "副标题",
  "author": "Mindflow AI",
  "date": "2025/11/3",
  "slides": [
    {
      "number": 1,
      "title": "页面标题",
      "content": "原始内容",
      "bullets": ["要点1", "要点2", "要点3"]
    }
  ]
}
```

### 对应的占位符

**标题页**：
- `{d.title}` → 演示文稿标题
- `{d.subtitle}` → 副标题
- `{d.author}` → 作者
- `{d.date}` → 日期

**内容页（循环）**：
- `{d.slides[i].title}` → 第 i 页的标题
- `{d.slides[i].bullets[j]}` → 第 i 页的第 j 个要点

---

## 🧪 测试占位符是否正确

编辑并上传模板后，测试：

```bash
curl -L -X POST "https://api.carbone.io/render/新的templateId?download=true" \
  -H "Authorization: Bearer ..." \
  -H 'carbone-version: 4' \
  -H 'Content-Type: application/json' \
  -d '{
    "data": {
      "title": "测试标题",
      "subtitle": "测试副标题",
      "author": "测试作者",
      "date": "2025-11-03",
      "slides": [
        {"number": 1, "title": "第一页", "bullets": ["要点1", "要点2"]},
        {"number": 2, "title": "第二页", "bullets": ["内容A", "内容B"]}
      ]
    },
    "convertTo": "pptx"
  }' \
  --output /tmp/test-final.pptx

# 打开查看
open /tmp/test-final.pptx
```

**检查**：
- ✅ 首页标题是否显示为"测试标题"
- ✅ 首页副标题是否显示
- ✅ 内容页是否有"第一页"、"第二页"
- ✅ 列表项是否正确

---

## 💡 快速提示

### 如果不想编辑模板

**临时方案**：将标题信息作为第一张幻灯片传递

修改 AI 提示词，让它生成包含标题页的幻灯片数组：

```json
slides: [
  {
    "title": "演示文稿标题",
    "content": "副标题\n作者\n日期"
  },
  {
    "title": "第一页内容",
    "content": "..."
  }
]
```

这样即使首页占位符不对，也能通过 `{d.slides[0].title}` 显示标题。

---

## 🎯 推荐操作

### 方案A：编辑模板（最佳，10分钟）

- ✅ 保留完整的 Google Slides 设计
- ✅ 首页标题正确显示
- ✅ 专业效果

### 方案B：使用我创建的简单模板（临时，0分钟）

- Template ID: 4e8cd3240e5a00b02fb4cf91cfd55b22915146b89e56a4b9848ad68b12818edd
- 简单但可用
- 等待时间来编辑 Google Slides 模板

---

**建议**：先用简单模板让功能可用，后续再优化 Google Slides 模板！

只需在 Vercel 环境变量中设置：
```
CARBONE_PPT_TEMPLATE_ID=4e8cd3240e5a00b02fb4cf91cfd55b22915146b89e56a4b9848ad68b12818edd
```

（旧的简单模板，标题页和内容页都能正常工作）

---

**或者**：

现在就编辑 Science project.odp，添加 `{d.title}` 和 `{d.subtitle}` 到首页！

